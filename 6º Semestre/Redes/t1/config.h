#define N_ROUT 7 			// Numero de Roteadores
#define BOX_SIZE 1000 		// Quantidade de mensagens que cada roteador pode receber
#define MENSAGEM_SIZE 100 	// Tamanho máximo em caracteres de cada mensagem
#define ASCII 65 			// Valor na tabela ascii para printagem de letras
#define TIPO "%c" 			// Tipo a ser printado

//Defines usados na priority_queue
#define INF 112345678
#define ii(a, b) leTemp.first = (a), leTemp.second = (b)
#define it(a, b) pqueue.key = (a), pqueue.value = (b)